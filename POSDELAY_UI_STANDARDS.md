# PosDelay UI 기준

## 공통 기준
- 모든 최상위 영역은 `상황`, `제어`, `임계값`, `진단` 중 하나에 속한다.
- 카드는 제목, 현재 상태 요약, 접힘/펼침 동작이 있어야 한다.
- 지속 설정을 바꾸는 카드는 `자동저장`을 기본 방식으로 한다.
- 직접 실행 버튼은 저장 대상이 아니라 즉시 명령이다. 예: 광고 즉시 실행, 배달료 기준값/미만값 적용, PC 전원.
- 숫자 입력만 강제하지 않는다. 임계값 성격에 따라 게이지, 현재값, 남은 건수, 소스 표시를 같이 쓴다.

## 카테고리 기준
- 상황: 실시간 값, 수집 시각, 데이터 소스, 서브폰/PC 상태가 보여야 한다.
- 제어: ON/OFF 토글 또는 실행 버튼이 명확해야 하고, 지속 설정은 자동저장 표시가 있어야 한다.
- 임계값: 기준 소스, 현재값, 설정값, 적용 모드, 자동저장 상태가 보여야 한다.
- 진단: 운영 설정과 분리하고, 로그/차트/상태 확인만 둔다.

## 임계값 카드 추가 기준
- 어떤 건수를 보는지 먼저 표시한다. 예: KDS 처리중, 프린터 30분 활성건수.
- 현재값과 설정값을 같은 화면에서 비교할 수 있어야 한다.
- 일반/1인근무처럼 모드가 나뉘면 같은 카드 안에서 나란히 보여야 한다.
- 피크시간처럼 여러 기능에 같이 적용되는 기준은 별도 운영모드 카드에 둔다.
- 저장 버튼을 새로 만들지 않는다. 값 변경 후 자동저장으로 통일한다.
- 위험한 실제 실행 버튼은 설정 입력과 시각적으로 분리한다.

## 2026-05-28 점검 결과
- 상황: 기준 충족. 실시간 KDS, 프린터, 가게배달 값과 수집 시각 표시.
- 제어: 조리모드, 광고, 스케줄, 할인, 지연 설정에 자동저장 표시 추가.
- 임계값: 배달료 조정은 프린터 30분 활성건수 고정. 임계값 이상은 기준이상 금액, 임계값 미만과 0건은 미만/0건 금액을 적용한다. 금액은 0~4000원 500원 단위.
- 운영모드: 피크시간을 가게배달 시간 카드에서 분리해 자동수락/영업정지 공통 기준으로 표시.
- 요약: ON 항목을 OFF 항목보다 위에 정렬하고, 데이터 없는 주문 영역은 작게 접는다.
- 진단: 운영 설정과 분리 유지.
